package fr.xgouchet.elmyr

/**
 * Describes the different case a Char can be in.
 */
enum class Case {
    /** Any case (lower or upper). */
    ANY,
    /** Lower case only. */
    LOWER,
    /** Upper case only. */
    UPPER
}
